enum paths {
  "AUTH" = "/auth",
  "MESSAGES" = "/messages",
  "LOGIN" = "/login",
  "LOGOUT" = "/logout",
  "SIGNUP" = "/signup",
  "ME" = "/me",
  "SENTBYID" = "/send/:id",
  "CONVERSATIONS" = "/conversations",
  "GETMESSAGESBYID" = "/:id",
}

export default paths;
